services:
  nginx:
    image: nginx:alpine
    container_name: baseplate-nginx
    entrypoint: ["/usr/local/bin/nginx-entrypoint.sh"]
    ports:
      - "${NGINX_PORT:-9083}:8080"
    environment:
      - TZ=Asia/Seoul
    volumes:
      - ./docker/nginx-entrypoint.sh:/usr/local/bin/nginx-entrypoint.sh:ro
      - ./docker/nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf
      - .:/var/www/html
    depends_on:
      - baseplate-php

  baseplate-php:
    build: ./docker
    container_name: baseplate-php
    restart: always
    user: "1000:1000"
    expose:
      - "${PHP_PORT:-9000}"
    environment:
      - TZ=Asia/Seoul
    volumes:
      - ./docker/php.ini:/usr/local/etc/php/php.ini
      - ./docker/php-fpm.d/www.conf:/usr/local/etc/php-fpm.d/www.conf
      - .:/var/www/html
    depends_on:
      - baseplate-redis

  baseplate-redis:
    image: redis:alpine
    container_name: baseplate-redis
    restart: always
    ports:
      - "6079:6379"
    command: redis-server

volumes:
  redis_data:
